---
name: research-project-orchestrator
description: 为科研、论文、期刊筛选、文献整理、公众号和自动化等多阶段项目建立可恢复的计划、任务交接与验收流程，按 Astra 规划和终审、Luna 批量执行、Sol 综合写作分工。适用于需要持续跟踪的项目；单次问答或简单润色无需启动完整流程。
---

# Research Project Orchestrator

以中文协作，保留英文文件名。把用户目标转成有证据、可验收、可续接的成果。用户的明确要求优先于本 Skill 的工作建议；遵守所在环境的权限与工具约束。不要把本 Skill 当成发布、发送消息、购买服务或启用定时任务的授权。

## 1. 启动与恢复

**READ BEFORE ACTING.** 在用户指定的项目目录读取已有 `PROJECT_PLAN.md`、`PROGRESS.md` 与当前任务；不要重建或覆盖已完成的项目记录。首次启动时，从本 Skill 的 `assets/templates/` 复制必要模板到项目目录，填写实际信息。模板目录只作为原件，不存放项目运行状态。

从现有上下文提取：目标、受众、输入材料、交付格式、范围边界、截止时间、质量要求、预算（如用户提供）、已经确定不再修改的内容。只询问会实质改变执行的缺失信息，一次优先问 1–3 个；其余记录为可撤销假设，继续不依赖答案的工作。缺少原始数据或全文时，不编造补齐。

按需读取支持文件：
- 涉及 PDF：读 [references/pdf-to-markdown.md](references/pdf-to-markdown.md)。
- 科研、论文、期刊、文献、公众号或自动化任务：只读 [references/domain-workflows.md](references/domain-workflows.md) 中相关部分。
- 模型切换或环境适配：读 [references/model-routing.md](references/model-routing.md)。

小项目可仅使用计划、进度和终审清单；复杂项目再增加任务文件、风险记录和交接文件。不要为流程本身制造工作。

## 2. 模型分工

这是本项目的路由策略，不是对模型能力的排他性定义。

| 角色 | 建议模型 | 负责内容 | 升级信号 |
|---|---|---|---|
| Planner / Final Reviewer | Astra | 目标与证据边界、任务依赖、风险、最终验收 | 目标冲突、重大新证据或范围变化时重新规划 |
| Batch Executor | Luna | 字段提取、已定义规则下的格式转换、批量检查、明确的小改动 | 规则含糊、来源冲突、需要综合判断时交 Sol；影响研究结论则交 Astra |
| Writer / Integrator | Sol | 复杂写作、跨来源综合、整合结果、协调术语与结构 | 证据不足以支持中心结论、研究设计或范围需改变时交 Astra |

**VERIFY ACTUAL MODEL.** 在进度中分别记录建议模型和实际模型；实际模型不可见时写“未知”，不以角色扮演冒充切换。工具支持且获准时才显式指定目标模型。没有切换工具时生成 `HANDOFF.md`，给出下一阶段建议；当前模型能胜任且不违背用户要求时继续，并记录偏离。用户明确要求必须使用某模型时，不擅自替代。

默认按阶段串行推进。只有环境允许、用户授权且任务可独立时才委派；各执行者仅写自己的任务产物，由主协调者更新共享计划和进度。不得为实现角色分工自行新建用户可见聊天或定时任务。

## 3. 规划：Astra

**PLAN TO ACCEPTANCE.** 填写 `PROJECT_PLAN.md`：
1. 用一句话定义成果，列入范围、排除范围和冻结决定。
2. 登记输入及可用性，为材料分配稳定来源 ID。
3. 定义交付物与可检查的验收条件，说明核验方法和证据位置。
4. 拆为有 ID 的任务，记录依赖、建议模型、输出路径、风险、完成条件。
5. 列出影响结论、完整性、权限、时间的实际风险，明确触发信号和应对。

任务粒度以“单次可交接、结果可独立验收”为准。不要把整篇论文写作交给 Luna，也不要把机械提取任务默认升为高复杂度研究。默认不新增用户未要求的时间或 token 总预算；估算与实测分开。

规划进入执行的门槛：当前任务所需输入可用、依赖满足、验收条件明确、没有阻止该任务的关键问题。除非用户要求先审计划，不必在每阶段重复索要批准。

## 4. 执行：Luna 与 Sol

每项任务使用 [TASK_TEMPLATE.md](assets/templates/TASK_TEMPLATE.md)，只加载相关约束、来源片段和依赖结果。

**PILOT BEFORE BATCH.** 批量任务先取代表性样本，覆盖正常项和异常项；样本规模按异质性确定。验证字段、定位和转换质量后扩展。使用确定性工具完成排序、去重、计数等机械步骤，避免逐条重新推理。

**PRESERVE EVIDENCE.** 提取内容保留来源 ID、页码或章节、核验状态；区分原文事实、作者主张、模型推断和建议。论文数字、引用与结果不可无依据改动。缺失字段标“未找到”；未访问的全文不能标“全文核验”。

**INTEGRATE WITH TRACEABILITY.** Sol 依据已核验材料整合，保留证据映射；写作发现冲突时回查来源或交回规划，不通过修辞掩盖缺口。沿用作者术语、语气和冻结决定。

每次任务完成、失败、阻塞或范围变化后更新 `PROGRESS.md`；任务有实际输出且通过验收才标 `DONE`。长任务在中断前保存检查点，避免每一步生成重复日志。

## 5. 状态、切换与变更

任务状态：`TODO → READY → RUNNING → REVIEW → DONE`。
- `READY`：依赖和必要输入齐全。
- `REVIEW`：已产出，等待对应验收，不能计为完成。
- `BLOCKED`：记录具体阻塞、已尝试方法、解除条件，继续独立任务。
- `REWORK`：审查未通过，绑定问题 ID，修复后重回 `REVIEW`。
- `CANCELLED`：仅用于明确取消或经记录的范围移除，不计作已完成交付。

项目阶段：`PLANNING → EXECUTION → INTEGRATION → FINAL_REVIEW → COMPLETE`。无需整合的项目可跳过 `INTEGRATION`，记录理由。终审发现问题返回相关任务；所有约定验收通过且无阻塞问题才进入 `COMPLETE`。

**CHANGE WITH A RECORD.** 新信息改变范围、验收或关键结论时，更新计划版本和决策记录，标出受影响任务，失效的旧核验重新执行。保留旧产物或版本，不静默修改验收条件来使任务“通过”。

## 6. 错误与阻塞

先分类：工具故障、访问缺失、数据缺失、来源矛盾、能力不足、权限限制。
- 暂时性只读故障可有限重试，默认最多额外 2 次；无进展后换方法或阻塞该任务。
- 写入、提交或发送结果不确定时先核对外部状态；不得盲目重试造成重复操作。
- 推理或规则错误先查根因，不重复同一提示期待不同结果；按模型表升级。
- 来源冲突保留双方证据，标明对结论的影响，不能自行择一伪装成确定事实。
- 每个阻塞记录错误、影响任务、尝试结果、下一步及所需用户信息。仅在实际缺少必要信息或授权时询问用户。

中断或换模型前填写 [HANDOFF.md](assets/templates/HANDOFF.md)：当前目标、计划版本、已完成证据、下一项 READY 任务、冻结决定和未解问题。新阶段只读交接、计划与相关任务，必要时再查历史。

## 7. 最终审查：Astra

用 [REVIEW_CHECKLIST.md](assets/templates/REVIEW_CHECKLIST.md) 审核最终版本，而不只是执行摘要：逐项验收，回查中心结论、关键数值、引用、图表和来源对应。批量普通字段可按风险抽样，关键结果及已知异常全查；记录覆盖范围，不把抽样说成全量核验。

为问题分级：`BLOCKER` 阻止交付；`MAJOR` 必须修复；`MINOR` 可修复或明示残留。修复后检查受影响部分与相关回归，避免无理由无限审查。角色分离不代表统计意义上的独立审查；记录审查实际模型和方法。

最终交付包括：成果链接、已通过的验收及证据、实际残留限制、必要下一步。存在未通过的必要验收时明确标“部分完成/待处理”，不得宣布完整完成。交付与对外发布分开；发布仅按现有明确授权执行。

